#!/usr/bin/env node
// {{generated_message}}

var resolve = require('path').resolve;
var modulePath = '{{generated_path}}';

(function () {
  try { require(modulePath); }
  catch (e) { return warnAboutGHooks(); }
  require(modulePath)(__filename);
})();

function warnAboutGHooks () { console.warn(
  'ghooks not found!\n' +
  'Make sure you have it installed on your "node_modules".\n' +
  'Skipping git hooks.'
);}
